{
  "name": "moonbeam_transactions",
  "version": "0.0.4",
  "description": "This SubQuery project tracks balance transfers in Moonbeam",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "homepage": "https://github.com/subquery/moonbeam-subql-starter",
  "repository": "github:subquery/moonbeam-subql-starter",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "PureStake",
  "license": "MIT",
  "devDependencies": {
    "@polkadot/api": "^6",
    "@subql/types": "latest",
    "typescript": "^4.1.3",
    "@subql/cli": "latest",
    "moonbeam-types-bundle": "^2.0.3"
  },
  "exports": {
    "chaintypes": "src/chaintypes.ts"
  }
}